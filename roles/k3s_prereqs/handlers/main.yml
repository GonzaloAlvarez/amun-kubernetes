---
- name: Reboot for cgroup
  ansible.builtin.reboot:
    msg: "Reboot to apply cgroup memory cmdline change"
    reboot_timeout: 300
    pre_reboot_delay: 5
    post_reboot_delay: 30
    test_command: uptime

- name: Reload ufw
  ansible.builtin.command: ufw reload
  failed_when: false
